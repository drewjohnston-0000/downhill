{
	"recommendations": [
		// Linter: runs system gdtoolkit gdlint + .gdlintrc = live editor linting
		// identical to CI.
		"eddiedover.gdscript-formatter-linter",
		// Formatter (GDScript) + Godot language server.
		"dohe.godot-format",
		"geequlim.godot-tools"
	]
}
